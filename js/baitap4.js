//Bài 4

/**
 * Khối 1: input
 *    width
 *    length
 * Khối 2:
 *    Bước 1: tạo biến và gán giá trị khởi tạo
 *    Bước 2: xây dựng công thức tính toán
 *    s = length * width
 *    p = (length + width) * 2
 *    Bước 3: hiển thị kết quả
 * Khối 3: output
 *    s (diện tích)
 *    p (chu vi)
 */

//Bước 1:
var width = 3;
var length = 4;
var s = 0;
var p = 0;

//Bước 2;
s = length * width
p = (length + width) * 2

//Bước 3
console.log("Bài 4:")
console.log("Diện tích hình chử nhật: " + s + " m")
console.log("Chu vi hình chử nhật: " + p + " m")